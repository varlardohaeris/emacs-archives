;;; Commentary:

;; `M-x aumix' runs the "aumix" mixer program (sound volume adjuster) in a
;; buffer.  This is the same as in a tty, but within Emacs.  See the
;; docstrings of `aumix' and `aumix-mode' for details.

